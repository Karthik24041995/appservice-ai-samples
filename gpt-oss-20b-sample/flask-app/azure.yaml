name: python-webapp-acr
metadata:
  template: python-webapp-acr
infra:
  provider: bicep
  path: ./infra/main.bicep

services:
  web:
    project: .
    language: python
    host: appservice
    module: web
